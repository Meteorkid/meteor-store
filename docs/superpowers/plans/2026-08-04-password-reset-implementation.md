# 密码重置实施计划

1. 为重置令牌编写签发、过期、用途隔离和版本校验测试。
2. 实现独立的 password-reset token 模块。
3. 为请求重置接口编写防枚举和仅向已验证账户发信测试。
4. 实现 Resend 密码重置邮件及请求接口。
5. 为提交重置接口编写成功、弱密码、重复使用和不签发会话测试。
6. 实现条件更新密码与 `tokenVersion` 的提交接口。
7. 接通登录页入口、忘记密码页和重置密码页，中英文文案同步。
8. 运行类型检查、Lint、单元测试和 Production build，分主题提交并部署验收。
